name: ucp_dart_sdk
description:  Unofficial Dart library for the Universal Commerce Protocol (UCP).
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.4

dependencies:
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1

dev_dependencies:
  build_runner: ^2.4.7
  freezed: ^2.4.6
  json_serializable: ^6.7.1
  lints: ^6.0.0
  test: ^1.25.6
